Solr Web User Interface

It was build with Ruby on Rails.

To run application

First you need run the following command in the rails application directory

bundle install

To start rails web server

rails server

Then go to url,
0.0.0.0:3000